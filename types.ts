export interface NavItem {
  label: string;
  href: string;
}

export interface ServiceItem {
  title: string;
  description: string;
  subtext: string;
}

export interface Testimonial {
  quote: string;
  author: string;
}